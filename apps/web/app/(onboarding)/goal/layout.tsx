'use client';

// ═══════════════════════════════════════════
// GOAL LAYOUT — Shared Step 2 wrapper
// No extra styling needed — inherits from onboarding layout
// ═══════════════════════════════════════════

export default function GoalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
